/**
 * 使一个函数遍历对象数组（第一个参数）并返回具有匹配的名称和值对的所有对象的数组（第二个参数）。
 * 如果要将源对象的每个名称和值对包含在返回的数组中，则该对象必须存在于集合的对象中。

  例如，如果第一个参数为[{ first: "Romeo", last: "Montague" }, { first: "Mercutio", last: null }, { first: "Tybalt", last: "Capulet" }]，
  第二个参数为{ last: "Capulet" }，则必须从数组中返回第三个对象（第一个参数），因为它包含名称和其值，该对象作为第二个参数传递。
 */
